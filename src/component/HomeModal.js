import React from 'react';
import { View, SafeAreaView, Image, Text, StyleSheet, Modal } from 'react-native';

const HomeModal = () => {
    return (
        <Modal>

        </Modal>
    );
}

export default HomeModal;